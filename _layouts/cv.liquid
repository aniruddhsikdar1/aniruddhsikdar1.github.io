---
layout: default
---
<style>
/* ══════════════════════════════════════════
   CV — Clean Minimal Design
   ══════════════════════════════════════════ */

/* ── Section cards ── */
div.cv div.card {
  border: 1px solid var(--global-divider-color) !important;
  border-radius: 8px !important;
  box-shadow: none !important;
  margin-top: 1.2rem !important;
  overflow: visible !important;
}

/* ── Section headings ── */
div.cv h3.card-title {
  font-size: 0.72rem !important;
  font-weight: 700 !important;
  text-transform: uppercase !important;
  letter-spacing: 0.12em !important;
  color: var(--global-text-color-light) !important;
  border-bottom: 1px solid var(--global-divider-color) !important;
  padding-bottom: 0.4rem !important;
  margin-bottom: 0 !important;
}

/* ── List items (timeline rows) ── */
div.cv .list-group-item {
  background: transparent !important;
  border: none !important;
  border-bottom: 1px solid var(--global-divider-color) !important;
  padding: 0.9rem 0.3rem !important;
}
div.cv .list-group-item:last-child {
  border-bottom: none !important;
}

/* ── Year / date badge — muted pill ── */
div.cv .badge.danger-color-dark {
  background: transparent !important;
  color: var(--global-text-color-light) !important;
  border: 1px solid var(--global-divider-color) !important;
  border-radius: 5px !important;
  font-size: 0.65rem !important;
  font-weight: 600 !important;
  padding: 3px 8px !important;
  letter-spacing: 0.02em !important;
  min-width: 70px !important;
  text-align: center !important;
  box-shadow: none !important;
  text-transform: none !important;
  line-height: 1.5 !important;
}

/* ── Entry title ── */
div.cv h6.title {
  font-size: 0.95rem !important;
  font-weight: 700 !important;
  color: var(--global-text-color) !important;
  margin-bottom: 0.2rem !important;
  line-height: 1.4 !important;
}

/* ── Institution / department ── */
div.cv table.institution {
  margin-bottom: 0.4rem !important;
}
div.cv td.institution {
  font-size: 0.85rem !important;
  line-height: 1.5 !important;
  color: var(--global-text-color) !important;
}
div.cv td.department {
  font-size: 0.79rem !important;
  color: var(--global-text-color-light) !important;
}

/* ── Description bullets ── */
div.cv ul.items {
  margin-top: 0.3rem !important;
  padding-left: 1rem !important;
}
div.cv ul.items li .item,
div.cv ul.items li .subitem {
  font-size: 0.86rem !important;
  line-height: 1.6 !important;
  color: var(--global-text-color) !important;
}
div.cv ul.items li .item-title {
  font-size: 0.88rem !important;
  font-weight: 600 !important;
}

/* ── General Info map table ── */
div.cv .table-cv.table-sm td {
  padding: 0.3rem 0.45rem !important;
  font-size: 0.87rem !important;
  line-height: 1.5 !important;
  vertical-align: middle !important;
}

/* ── Link / download buttons ── */
div.cv a.btncv {
  border-radius: 4px !important;
  font-size: 0.67rem !important;
  padding: 2px 9px !important;
  transition: background 0.15s, color 0.15s !important;
}
div.cv a.btncv:hover {
  background: var(--global-theme-color) !important;
  color: #fff !important;
  border-color: var(--global-theme-color) !important;
}

/* ── Location ── */
div.cv p.location {
  font-size: 0.67rem !important;
  margin-top: 3px !important;
  color: var(--global-text-color-light) !important;
}
</style>

{% unless site.data.resume %}
  <div class="post">
    <header class="post-header">
      <h1 class="post-title">
        {{ page.title }}
        {% if page.cv_pdf %}
          <a
            {% if page.cv_pdf contains '://' %}
              href="{{ page.cv_pdf }}"
            {% else %}
              href="{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url }}"
            {% endif %}
            target="_blank"
            rel="noopener noreferrer"
            class="float-right"
          >
            <i class="fa-solid fa-file-pdf"></i>
          </a>
        {% endif %}
      </h1>
      {% if page.description %}
        <p class="post-description">{{ page.description }}</p>
      {% endif %}
    </header>

    <article>
      <div class="cv">
        {% for entry in site.data.cv %}
          <a class="anchor" id="{{ entry.title }}"></a>
          <div class="card mt-3 p-3">
            <h3 class="card-title font-weight-medium">{{ entry.title }}</h3>
            <div>
              {% if entry.type == 'list' %}
                {% include cv/list.liquid %}
              {% elsif entry.type == 'map' %}
                {% include cv/map.liquid %}
              {% elsif entry.type == 'nested_list' %}
                {% include cv/nested_list.liquid %}
              {% elsif entry.type == 'time_table' %}
                {% include cv/time_table.liquid %}
              {% elsif entry.type == 'list_groups' %}
                {% include cv/list_groups.liquid %}
              {% else %}
                {{ entry.contents }}
              {% endif %}
            </div>
          </div>
        {% endfor %}
      </div>
    </article>
  </div>
{% else %}
  <div class="post">
    <header class="post-header">
      <h1 class="post-title">
        {{ page.title }}
        {% if page.cv_pdf %}
          <a
            {% if page.cv_pdf contains '://' %}
              href="{{ page.cv_pdf }}"
            {% else %}
              href="{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url }}"
            {% endif %}
            target="_blank"
            rel="noopener noreferrer"
            class="float-right"
          >
            <i class="fa-solid fa-file-pdf"></i>
          </a>
        {% endif %}
      </h1>
      {% if page.description %}
        <p class="post-description">{{ page.description }}</p>
      {% endif %}
    </header>

    <article>
      <div class="cv">
        {% for data in site.data.resume %}
          {% if site.jsonresume and site.jsonresume.size > 0 %}
            {% unless site.jsonresume contains data[0] %}
              {% continue %}
            {% endunless %}
          {% endif %}
          {% if data[0] == 'meta' or data[1].size == 0 %} {% continue %} {% endif %}
          <a class="anchor" id="{{ data[0] }}"></a>
          <div class="card mt-3 p-3">
            <h3 class="card-title font-weight-medium">{{ data[0] | capitalize }}</h3>
            <div>
              {% case data[0] %}
                {% when 'basics' %}
                  {% include resume/basics.liquid %}
                {% when 'education' %}
                  {% include resume/education.liquid %}
                {% when 'work' %}
                  {% include resume/work.liquid %}
                {% when 'volunteer' %}
                  {% include resume/volunteer.liquid %}
                {% when 'projects' %}
                  {% include resume/projects.liquid %}
                {% when 'awards' %}
                  {% include resume/awards.liquid %}
                {% when 'skills' %}
                  {% include resume/skills.liquid %}
                {% when 'publications' %}
                  {% include resume/publications.liquid %}
                {% when 'languages' %}
                  {% include resume/languages.liquid %}
                {% when 'interests' %}
                  {% include resume/interests.liquid %}
                {% when 'certificates' %}
                  {% include resume/certificates.liquid %}
                {% when 'references' %}
                  {% include resume/references.liquid %}
                {% else %}

              {% endcase %}
            </div>
          </div>
        {% endfor %}
      </div>
    </article>
  </div>
{% endunless %}
